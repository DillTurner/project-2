create database cannabis_db;


use cannabis_db;

create Table Janes_data (
id int auto_increment,
name varchar (500),
race varchar (30),
flavor varchar (500),
positive_effects varchar (500),
negative_effects varchar(500),
medical_usage varchar(500),
primary key (id)
);